name: main workflow
on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: "Hello World!!!"
        run: |
          echo "Hello World... :)" 


  call-reusable:
    uses: ./.github/workflows/reusable.yml
    with:
      config-path: smiley/face/hehe.yml
