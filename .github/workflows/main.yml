name: main workflow
on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  hello-temp:
    name: "hello / pipeline"
    runs-on: ubuntu-latest
    steps:
        run: |
          echo "temporary job with name change"
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: "Hello World!!!"
        run: |
          echo "Hello World... :)"

  temp-job:
    runs-on: ubuntu-latest
    steps:
      - name: "Demo source branch changes"
        run: |
          echo "Hello World... :)"

  gcc:
    uses: ./.github/workflows/reusable.yml
    with:
      target-runner-labels: "['ubuntu-latest']"
      config-path: smiley/face/hehe.yml

  cuda:
    name: cuda-uvm
    uses: ./.github/workflows/reusable.yml
    with:
      target-runner-labels: "['ubuntu-latest']"
      config-path: some/cuda/gpu.yml

